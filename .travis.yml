language: php

services:
  - docker

before_install:
  - composer self-update
  - composer create-project --prefer-dist laravel/laravel laraship-app
  - mv laraship-app/* laraship-app/.* .
  - rm -rf laraship-app
  - ls -al

script:
  - docker-compose up -d
  - docker ps
  - docker ps -a --filter "status=running" -q -s | wc -l | xargs -I % test % -eq 4
